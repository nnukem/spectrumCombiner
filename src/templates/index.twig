{% extends "base.twig" %}

{% block title %}
    {{ title }}
{% endblock %}

{% block body %}
    {% for section in sections %}
        {%  import section.file as sec %}
        {{ sec.init(section.data,section.class) }}
    {% endfor %}
{% endblock %}
